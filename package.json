{
  "name": "slipgate-raid",
  "version": "1.0.0",
  "description": "A Quake-themed raid encounter simulator: one wipe-able boss, three bots, no build step.",
  "type": "module",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "sim": "node sim.js --runs 200",
    "test": "node --test tests/*.mjs"
  }
}
